# ===============================
# Application & Server
# ===============================
spring.application.name=DeserveIQ
server.port=8080

# ===============================
# PostgreSQL (Neon) Datasource
# ===============================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-quiet-cherry-af4vbqr8-pooler.c-2.us-west-2.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_R9vsZiY6WQpl}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Jackson config (snake_case)
# ===============================
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.mapper.accept-case-insensitive-properties=true

# ===============================
# ML Flask API base
# ===============================
ml.api.url=${ML_API_URL:http://localhost:5000}
ml.api.predict-endpoint=/predict